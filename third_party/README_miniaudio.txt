Place `miniaudio.h` here:

  third_party/miniaudio.h

Waveform playback will automatically switch from the MCI fallback to miniaudio
when this header is present (see `AudioEngine.cpp`).

Official source:
  https://github.com/mackron/miniaudio

